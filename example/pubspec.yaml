name: arabic_chat_example
description: Example app for Arabic Chat Roleplay package
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Use the local package
  arabic_chat_roleplay:
    path: ../
  
  # State management
  flutter_riverpod: ^2.5.1
  
  # UI
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true